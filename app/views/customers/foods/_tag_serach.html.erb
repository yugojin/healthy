<table class="table table-bordered">
  <thead><th class="text-center">タグ検索</th></thead>
    <tbody>
      <td>
        <% @tag_list.each do |list| %>
    			<p class="tag-serach text-center"><%=link_to list.name, search_path(food_tag_id: list.id) %></p>
    		<% end %>
      </td>
    </tbody>
</table>